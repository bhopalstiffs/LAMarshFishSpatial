#Removal estimates for fish species

library(FSA)

# Load "FishData.xlsx"
# Create a loop that will go through each row in the "FishData.xlsx" and calculate this function:
# removal(row vector, method = "Burnham", just.ests = TRUE), results will go in the total column in the "FishData.xlsx"

